.grid_8
  %p
    Price now is
    = @scrapes
    <br>
    Last price was 
    = @price_last.price
    <br>
    - if @scrapes.to_f.to_s == @price_last.price.to_s 
      Nothing new here
    - else
      New price !
